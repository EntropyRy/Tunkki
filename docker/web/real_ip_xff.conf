# Real IP configuration for X-Forwarded-For mode (dev/internal)
# Trust internal Docker networks and use X-Forwarded-For header

set_real_ip_from 172.16.0.0/12;
set_real_ip_from 10.0.0.0/8;
real_ip_header X-Forwarded-For;
real_ip_recursive on;
