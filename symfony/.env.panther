# Environment variables for Panther tests (browser-based with real JavaScript)
# This file is loaded when APP_ENV=panther

APP_ENV=panther
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rpanther'

# SQLite database for Panther tests
# Note: DATABASE_URL is overridden at runtime by PantherTestCase to use /dev/shm (RAM-based)
# This provides faster performance and avoids file permission issues
# Placeholder value for reference only (actual path set dynamically per test)
DATABASE_URL="sqlite:///:memory:"

# Trust localhost for Panther's built-in web server
TRUSTED_HOSTS='^localhost|127\.0\.0\.1$'
