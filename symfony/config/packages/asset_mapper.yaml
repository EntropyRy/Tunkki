framework:
  asset_mapper:
    # The paths to make available to the asset mapper.
    paths:
      - assets/
    excluded_patterns:
      - "scss/*.scss"
      - "assets/styles/scss/**"
      - "assets/styles/app.scss"
