{% extends 'emails/base.html.twig'%}
{% block content %}
    {% if user is defined %}
    <div>
        {{ user.rejectReason|nl2br }}
    </div>
    {% else %}
    <div>
        {{ email.body|raw }}
    </div>
    {% endif %}
{% endblock %}
