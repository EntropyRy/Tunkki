{% extends event.template %}
{% block event_section_name %}{{ 'event.section.safer_space'|trans }}{% endblock %}
{% block content %}
    {{ block('event_heading') }}
    {% set input = template_from_string(event.contentForTwig(app.request.locale)) %}
    <div class="content">
        {{ block('content_safer_space', input) ?? '' }}
    </div>
    {{ block('event_back_to_link') }}
{% endblock %}
