{% extends 'onecolumn.html.twig' %}

{% block content %}
<div class="col-12">
    <div class="col-12">
{{ form_start(form) }}
{{ form_widget(form) }}
<input class="btn btn-primary" type="submit" value="{{'submit'|trans|capitalize}}">
{{ form_end(form) }}
    </div>
</div>
{% endblock %}
